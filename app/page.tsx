import { Experience } from "@/components/Experience";

/**
 * app/page.tsx — the single entry point.
 * This is a Server Component: it simply renders the <Experience /> client component
 * which mounts the entire scene engine.
 */
export default function Page() {
  return <Experience />;
}
